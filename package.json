{
  "name": "recoil-logger-demo",
  "version": "0.2.0",
  "description": "Demo for recoil state manager and logger",
  "main": "index.js",
  "repository": "git@polemius.github.com:polemius/recoil-logger-demo.git",
  "author": "Ivan Menshykov <ivan.menshykov@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "parcel serve --cache-dir .cache -d dist index.html --open",
    "build": "parcel build --cache-dir .cache -d dist index.html",
    "deploy": "yarn build && gh-pages -d dist -b master"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "recoil": "^0.0.7",
    "recoil-logger": "^0.2.0"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
